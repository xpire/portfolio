import React from 'react';
import styled from 'styled-components';

const Page = styled.div`
    min-width: 100%;
    min-height: 100vh;
    background: grey;
    position: relative;
`;

export default Page;